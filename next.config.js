/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note
  // Note
  
  // Note
  images: {
    unoptimized: true,
  },
  
  // Note
  distDir: '.next',
}

module.exports = nextConfig
